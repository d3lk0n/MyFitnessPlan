<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingBottom="32dp">

    <TextView
        android:id="@+id/date_added_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?textAppearanceBody1"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Date Added"/>

    <TextView
        android:id="@+id/choose_exercise_text_view"
        android:layout_width="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_height="40dp"
        android:textAppearance="?textAppearanceBody1"
        android:text="@string/text_choose_exercise"
        app:layout_constraintLeft_toLeftOf="@id/date_added_text_view"
        app:layout_constraintTop_toBottomOf="@id/date_added_text_view" />

    <!--<View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/purple_200"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="parent"
        android:layout_marginBottom="10dp"
        android:paddingBottom="10dp"/>
        android:background="?android:attr/listDivider" />-->


</androidx.constraintlayout.widget.ConstraintLayout>